// Check if the 'language' variable is null or empty
if (string.IsNullOrEmpty(language))
{
    // If 'language' is null or empty, set a default value or perform an action
    language = "English"; // Default language set to English
    // Additional actions can be performed here if necessary
}
else
{
    // If 'language' is not null or empty, proceed with the current value
    // Code to execute when 'language' has a valid value can be placed here
}
